# ----------------------------------------------------------------------
# Tier 2.5: + D4 symmetry by augmentation
# ----------------------------------------------------------------------
#
# GENERATED by research/ablations/generate_configs.py -- do not hand-edit.
# Edit the fiducial config or the arm's entry in that script and re-run.
#
# Fiducial:
#   research/unit_test_variations/fourth_inloop_shearnet_d4_2drope/config.yaml
#
# Changed from the fiducial:
#   train.generation        ->  'upfront'
#   train.response          ->  <removed>
#   model.architecture      ->  'fork-like'
#   model.fusion            ->  'transformer'
#   model.galaxy_branch     ->  'research_backed'
#   model.psf_branch        ->  'forklens_psf'
#   model.head              ->  'gap'
#   train.d4_augment        ->  True
#   model.design            ->  <removed>
#   model.fusion_pos        ->  <removed>
#   model.d4_features       ->  <removed>
#   model.d4_depths_galaxy  ->  <removed>
#   model.d4_depths_psf     ->  <removed>
#   model.orbit_scan        ->  <removed>
#
# Why:
# The eight D4 transforms are applied as training augmentation
# rather than built into the architecture. The network is then encouraged toward
# the symmetry but not held to it, so the orientation-dependent additive bias is
# suppressed on average and not identically. The contrast with rung 6 is the
# paper's argument for construction over augmentation, and it is visible in the
# rotation test as well as in c2.
#
# Read this before trusting the result:
# This is a cumulative ladder, not a leave-one-out: each rung
# adds one component to the rung above it, so a row differs from the FIDUCIAL in
# several keys while differing from its immediate predecessor in one. Read the
# table by adjacent differences, not against the last row.
#
# `d4_augment` is an ablation control and is explicitly not for use with
# d4-fork-like, where the symmetry is already exact and the augmentation would be
# a no-op costing 8x the data.
# ----------------------------------------------------------------------
meta:
  model_name: 05_d4_augmentation
  description: 'Tier 2.5: + D4 symmetry by augmentation'
paths:
  root: /home/adfield/ShearNet/research/ablations/tier2/05_d4_augmentation
  psfex_model_file: /home/adfield/weak_lensing/superbit-lensing-jax-test/simulated_data/sim_utils/emp_psfs/emp_psfs_best/psfex_all_except_best
  train_catalog: /home/adfield/ShearNet/cosmos_catalog_train.fits
  eval_catalog: /home/adfield/ShearNet/cosmos_catalog_eval.fits
  checkpoint_dir: null
  plot_dir: null
image:
  stamp_size: 53
  pixel_scale: 0.141
  noise_sd: 12.719674
  normalize_images: true
psf:
  mode: superbit
  gaussian_fwhm: 0.5
  stamp_size: 53
  noise: 1.0e-06
galaxy:
  hlr_type: catalog
  flux_type: catalog
  hlr: catalog
  flux: catalog
model:
  architecture: fork-like
  fusion: transformer
  head: gap
  galaxy_branch: research_backed
  psf_branch: forklens_psf
  output_keys:
  - g1
  - g2
  - hlr
  - flux
  gap: false
  dropout: 0.0
  d_model: 64
  num_heads: 4
  num_self_attn_layers: 1
  ffn_dim: 256
  num_pool_heads: 4
train:
  seed: 42
  samples: 300000
  epochs: 60
  batch_size: 128
  learning_rate: 0.0001
  weight_decay: 0.01
  patience: 10
  val_split: 0.2
  eval_interval: 1
  loss_weights:
  - 1.0
  - 1.0
  - 1.0
  - 1.0
  ema_decay: 0.999
  plot: true
  backend: jax-galsim
  generation: upfront
  jax_fft_size: 256
  jax_batch_size: 128
  noise:
    min_sd: null
    max_sd: null
    condition: false
  d4_augment: true
eval:
  seed: 150
  n_obs: 200000
  gal_model: gauss
  evaluate:
    baseline: ngmix
    response_step: 0.01
    shear_true: 0.01
    component: both
    n_jackknife: 20
    shape_noise_cancel: 4
    psf_model: gauss
    c_convention: lin2026
    resample: jackknife
    psf_response_apply: none
    shearnet_metacal: true
    anacal_sigma_arcsec: 0.52
    anacal_stamp_size: 32
    anacal_epochs: 35
    shearnet_batch_size: 256
    ngmix_nproc: null
    catalog_level: paper
    output: benchmarking/evaluation.fits
