# ----------------------------------------------------------------------
# UT1: ideal Gaussian PSF, fixed size and flux
# ----------------------------------------------------------------------
#
# GENERATED by research/ablations/generate_configs.py -- do not hand-edit.
# Edit the fiducial config or the arm's entry in that script and re-run.
#
# Fiducial:
#   research/unit_test_variations/fourth_inloop_shearnet_d4_2drope/config.yaml
#
# Changed from the fiducial:
#   psf.mode                     ->  'ideal'
#   galaxy.hlr_type              ->  'constant'
#   galaxy.flux_type             ->  'constant'
#   galaxy.hlr                   ->  0.5
#   galaxy.flux                  ->  12258.97
#   train.response.orbit_weight  ->  0.0
#
# Why:
# Shear recovery in isolation. The PSF is a circular Gaussian of
# 0.5 arcsec FWHM and every galaxy has the same half-light radius and flux, so
# the only thing varying across the population is the intrinsic shape and the
# applied shear. A bias here is a bias in the estimator itself and cannot be
# blamed on the PSF model or on the size-flux distribution.
#
# Read this before trusting the result:
# The PSF orbit penalty is off at this rung and only at this
# rung, because an ideal circular PSF makes it vacuous. Every other response term
# is unchanged, so UT1 is still the same objective as UT2-UT4 in everything that
# can act.
#
# hlr and flux are constant here while `output_keys` still asks
# for them. That is deliberate -- the network must be identical across the ladder
# -- and it is safe: fit_normalizer guards a zero standard deviation (it
# substitutes 1.0), so the auxiliary targets contribute a constant term rather
# than a division by zero. They are simply trivial to predict at this rung.
# ----------------------------------------------------------------------
meta:
  model_name: d4_unit_first
  description: 'UT1: ideal Gaussian PSF, fixed size and flux'
paths:
  root: /home/adfield/ShearNet/research/unit_tests/first
  psfex_model_file: /home/adfield/weak_lensing/superbit-lensing-jax-test/simulated_data/sim_utils/emp_psfs/emp_psfs_best/psfex_all_except_best
  train_catalog: /home/adfield/ShearNet/cosmos_catalog_train.fits
  eval_catalog: /home/adfield/ShearNet/cosmos_catalog_eval.fits
  checkpoint_dir: null
  plot_dir: null
image:
  stamp_size: 53
  pixel_scale: 0.141
  noise_sd: 12.719674
  normalize_images: true
psf:
  mode: ideal
  gaussian_fwhm: 0.5
  stamp_size: 53
  noise: 1.0e-06
galaxy:
  hlr_type: constant
  flux_type: constant
  hlr: 0.5
  flux: 12258.97
model:
  architecture: d4-fork-like
  fusion: transformer
  head: attention
  galaxy_branch: shearnet-d4
  psf_branch: shearnet-d4
  output_keys:
  - g1
  - g2
  - hlr
  - flux
  gap: false
  dropout: 0.0
  fusion_pos: rope2d
  design: shearnet-d4
  d_model: 64
  num_heads: 4
  num_self_attn_layers: 1
  ffn_dim: 256
  num_pool_heads: 4
  d4_features:
  - 16
  - 48
  - 64
  d4_depths_galaxy:
  - 1
  - 2
  - 1
  d4_depths_psf:
  - 0
  - 1
  - 1
  orbit_scan: true
train:
  seed: 42
  samples: 300000
  epochs: 60
  batch_size: 128
  learning_rate: 0.0001
  weight_decay: 0.01
  patience: 10
  val_split: 0.2
  eval_interval: 1
  loss_weights:
  - 1.0
  - 1.0
  - 1.0
  - 1.0
  ema_decay: 0.999
  plot: true
  backend: jax-galsim
  generation: inloop
  jax_fft_size: 256
  jax_batch_size: 128
  response:
    gamma_weight: 0.01
    psf_weight: 0.1
    shift_weight: 0.01
    complement_weight: 0.001
    orbit_weight: 0.0
    isotropy_weight: 0.01
    every_n_steps: 4
    orbit_k: 4
    gamma_target: analytic
    batch: 32
    report: true
  noise:
    min_sd: null
    max_sd: null
    condition: false
eval:
  seed: 150
  n_obs: 200000
  gal_model: gauss
  evaluate:
    baseline: ngmix
    response_step: 0.01
    shear_true: 0.01
    component: both
    n_jackknife: 20
    shape_noise_cancel: 4
    psf_model: gauss
    c_convention: lin2026
    resample: jackknife
    psf_response_apply: none
    shearnet_metacal: true
    anacal_sigma_arcsec: 0.52
    anacal_stamp_size: 32
    anacal_epochs: 35
    shearnet_batch_size: 256
    ngmix_nproc: null
    catalog_level: paper
    output: benchmarking/evaluation.fits
