# `shearnet.utils.device`

> Module: `shearnet.utils.device`

JAX device selection helpers for ShearNet.

<a id="shearnet.utils.device.get_device"></a>

#### `get_device`

```python
def get_device()
```

Return (and print) the default JAX device, i.e. the CPU or GPU in use.
